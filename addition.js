const add = (x = 0, y = 0) => {
  // uncomment to pass other test cases
  // if (typeof x !== "number" || typeof y !== "number") {
  //   return;
  // }

  return x + y;
};

module.exports = {
  add,
};
